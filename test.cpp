#include <bits/stdc++.h>

#include "simul.h"

using namespace std;

int main() {
    string input;
    string output;
    // cin >> input >> output;
    input = "b.txt";
    output = "b_out.txt";
    cout << eval(input, output) << endl;
    return 0;
}