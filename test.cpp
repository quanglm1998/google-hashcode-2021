#include <bits/stdc++.h>

#include "simul.h"

using namespace std;

int main() {
    string input;
    string output;
    cin >> input >> output;
    cout << eval(input, output) << endl;
    return 0;
}