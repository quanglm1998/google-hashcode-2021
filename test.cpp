#include <bits/stdc++.h>

#include "simul.h"

using namespace std;

int main() {
    string input;
    string output;
    // cin >> input >> output;
    input = "c.txt";
    output = "c_out.txt";
    cout << eval(input, output) << endl;
    return 0;
}